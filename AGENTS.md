# AGENTS.md — Leap 2026 Codex Instructions

このリポジトリで作業するCodex / AI coding agent向けの共通指示。

---

## 1. 最初に読むもの

作業を始める前に、原則として以下を確認する。

1. `AGENTS.md`
2. `MASTER.md`
3. 作業対象に最も近い仕様・シナリオ・設計文書
4. 既存コード
5. 必要に応じて `docs/` 内の技術監査・調査資料

`MASTER.md` はLeap 2026の制作上の基準書である。

コードと `MASTER.md` が食い違う場合、旧版コードを自動的に正とみなさない。旧版は調査対象であり、2026版の完成仕様ではない。

---

## 2. プロジェクトの目的

旧作『Leap ～ときをこえて～』を2026年版として再構築する。

作品の中心は「過去を改変して正しい未来を作る」ことではない。

過去そのものを変えられなくても、後悔した出来事の意味を捉え直すことで、現在からの選び方は変えられる、という物語である。

実装上の都合で、この核を変更しない。

---

## 3. 現在の最優先目標

最優先はゲーム全編の再実装ではない。

Chapter 01～03相当の15～20分のVertical Sliceを完成品質で制作し、Leap 2026の「型」を検証すること。

Vertical Sliceの範囲：

高校時代の高台  
→ 歩との再会  
→ 夏祭り  
→ 人混みではぐれる  
→ 後悔

作業提案・リファクタ・新規実装は、まずこのVertical Sliceの完成に寄与するかで優先順位を判断する。

---

## 4. 技術上の確定事項と未決定事項

### 確定している方針

- まずiOS / App Store公開を想定する。
- 横16:9を基本候補とする。
- Safe Areaを考慮する。
- シナリオをデータとして管理し、表示エンジンが解釈する思想を残す。
- 本文、背景、BGM / SE、暗転、待ち、画面演出をデータで記述できる方向を目指す。
- セーブは旧版の「先頭から高速再生」ではなくsnapshot保存を候補とする。
- 文字サイズ、コントラスト、ログ、オート、スキップ、音量個別調整を初期設計に含める。

### 未決定

以下は勝手に確定しない。

- Unityを継続するか。
- 新しいUnityプロジェクトへ移植するか。
- Unity以外へ移るか。
- 18章構成を維持するか。
- 話者名を常時表示するか。
- 選択肢を導入するか。
- 顔を明確に見せる場面。
- 最終プレイ時間。
- BGM / SEの継承範囲。
- AI利用開示の最終文面・タイミング。

未決定事項に関わる場合は、既成事実として実装するのではなく、選択肢とトレードオフを提示する。

---

## 5. 旧版コードを扱う際のルール

旧版はUnity 2020.2.2f1上の独自ScriptEngineによるデータ駆動型実装だった。

旧版コードには価値があるが、無条件で残す必要はない。

調査時は機能・構造を以下の観点で分類する。

- KEEP：そのまま、または小修正で再利用可能。
- REFACTOR：思想・機能は有用だが整理が必要。
- REBUILD：目的は残すが実装は新規の方が合理的。
- REMOVE：2026版では不要。

「古いから全部捨てる」「動いているから全部残す」のどちらにも寄らない。

---

## 6. Codexの作業原則

### 6.1 調査を先に行う

既存コードへ触れる作業では、変更前に関連ファイル・呼び出し関係・データフローを確認する。

推測だけで変更しない。

分からない場合は、

- コードから確認できたこと
- 推測していること
- 判断材料が不足していること

を分けて説明する。

### 6.2 必要最小限の変更を優先する

依頼された目的に必要な範囲だけ変更する。

避けるもの：

- 依頼に関係のない大規模リファクタ。
- Formattingだけの大量差分。
- ファイルの一括移動。
- 不要な命名変更。
- 「ついで」の全面設計変更。
- 大量の依存Package追加。

Vertical Slice段階では、理論上美しい巨大アーキテクチャより、理解しやすく壊れにくい構造を優先する。

### 6.3 一人開発を前提にする

このプロジェクトは小規模な個人開発を前提とする。

優先順位：

1. 理解しやすい。
2. 壊れにくい。
3. シナリオ追加・修正が容易。
4. Codexが将来も扱いやすい。
5. 数年後に人間が読んでも追える。
6. 必要十分なテストがある。

複雑な抽象化、過剰なDI、過剰なレイヤー分割、将来使うか不明な汎用フレームワーク化は避ける。

---

## 7. シナリオ / ゲーム進行の扱い

シナリオはコードへ直接埋め込むより、データとして管理する方向を優先する。

新しいシナリオ命令やデータ形式を設計する場合は、少なくとも以下を考慮する。

- 本文。
- 背景。
- キャラクター表示。
- BGM。
- SE。
- 暗転 / フェード。
- 待ち。
- 入力待ち。
- 画面演出。
- ログ。
- オート。
- スキップ。
- セーブ / ロード。
- 将来の命令追加。

Scenario層とUnity / UI実装の結合を必要以上に強くしない。

ただし、Vertical Sliceに不要な汎用化は行わない。

---

## 8. Save / Load

旧版の「開始位置から高速再生して状態を再構築する」方式を前提にしない。

2026版ではsnapshot型を候補とする。

保存候補：

- 章。
- シナリオ位置。
- 必要なフラグ。
- 背景。
- キャラクター / 表示状態。
- 音状態。
- 演出状態。
- その他、ロード後に同じ画面へ復帰するために必要な最小状態。

実装前に、永続化すべき「ゲーム状態」と、再計算可能な「表示状態」を区別する。

---

## 9. UI / 演出上の制約

Leap 2026の基本UIは「薄い暗転＋前面に文章が浮かぶ」サウンドノベル型。

大きな常設メッセージウィンドウで背景画を分断しない。

UI実装では以下を守る。

- 背景を生かす。
- 本文は白～薄い生成りの明朝系を第一候補。
- 行間を広めにする。
- 文章は左上～中央左を基本領域とする。
- 重要な被写体を避ける。
- AUTO / LOG / CONFIG等は小さくする。
- 重要な一文ではUIをさらに減らせる構造にする。
- Safe Areaに対応する。
- 文字サイズ・コントラスト変更を考慮する。

ビジュアル演出は「顔を描かない」ではなく「顔に頼らない」。

POSE / GESTURE / FRAMING / BODY DIRECTION / LIGHT / FOCUS / AMBIENCE / PAUSEを活用する。

---

## 10. シナリオ内容を変更する際の制約

コード修正の都合で台詞・テーマ・キャラクター性を勝手に変更しない。

シナリオを扱う場合は `MASTER.md` の以下を守る。

- 心情を文章だけで重複説明しない。
- 画・音・間で伝わる説明は削る。
- 一タップ一短文を機械的に続けない。
- マリーの設定を一度に説明し切らない。
- 歩をテーマ代弁装置にしない。
- 歩の快活さ・普通っぽさ・個人的感情を優先する。
- 「時間は巻き戻せるわけじゃないんだもん」という逆説を守る。
- 2年後の再会を過去改変の報酬にしない。

---

## 11. 生成AI / 画像関連

生成AIは制作工程の道具であり、作品の企画主体ではない。

コード側で生成AI素材を扱う場合でも、以下の記録方針を壊さない。

- 採用理由を残す。
- 不採用理由を残す。
- 同一キャラクターの基準画像を維持する。
- 使用サービス・生成日・商用利用条件等を記録できるようにする。
- 公開時のAI利用を隠すことを前提に設計しない。

特定作家の作風模倣をプロジェクト要件として追加しない。

---

## 12. 作業前の確認

ある程度の変更を伴うタスクでは、実装前に短く以下を整理する。

- 何を変更するか。
- なぜ必要か。
- どのファイルへ影響するか。
- 既存仕様との衝突はないか。
- 未決定事項を勝手に確定していないか。

大きな作業では先に調査結果・実装方針を提示する。

---

## 13. 作業後の確認

変更後は可能な範囲で以下を行う。

- Build / compile。
- 関連テスト。
- 主要な実行フロー確認。
- `git diff` の確認。
- 不要な変更が混ざっていないか確認。
- 警告・エラーの確認。

Unity環境やiOS環境が実行できず検証できない場合は、「検証済み」と書かず、未検証であることを明示する。

---

## 14. Git / 差分ルール

- ユーザーの既存変更を勝手に破棄しない。
- 依頼されていない `git reset --hard`、強制checkout、履歴書き換えを行わない。
- Secret、API key、証明書、署名情報をcommitしない。
- 生成物・キャッシュ・IDE固有ファイルを不用意に追加しない。
- 差分は目的ごとに小さく保つ。
- 大規模変更では、可能なら段階的なcommit単位を提案する。

Codex自身がcommitする必要はない。依頼された場合のみcommitする。

---

## 15. ドキュメント更新

以下の場合は関連ドキュメント更新を検討する。

- 技術方針を決定した。
- Vertical Sliceで仮説が検証された。
- シナリオデータ形式を変更した。
- Save / Load仕様を決定した。
- UI仕様を確定した。
- 新しい制作ルールを採用した。
- 既存ルールを撤回した。

ただし、未検証の仮説を `MASTER.md` の確定仕様へ勝手に昇格させない。

`MASTER.md` を更新する場合は、何を・なぜ変更したか分かるようにする。

---

## 16. 技術監査フェーズの特別ルール

初回の旧版技術監査では、大規模変更を行わない。

重点調査対象：

- Unityバージョン。
- Package / Plugin。
- Scene構成。
- Entry point。
- ScriptEngine。
- Scenario format。
- Parser / Interpreter。
- GameState。
- Text / Background / Character表示。
- Audio。
- Fade / Wait。
- Input。
- Save / Load。
- Config。
- Backlog。
- Auto。
- Skip。
- Canvas / Resolution。
- Platform依存処理。

調査結果では、ファイル名・クラス名・実際のコードを根拠にする。

技術監査の成果物は原則として：

`docs/LEAP_2026_TECH_AUDIT.md`

にまとめる。

---

## 17. Codexから提案してよいこと

以下は積極的に提案してよい。

- 既存コードのリスク。
- Vertical Sliceに必要な最小アーキテクチャ。
- Keep / Refactor / Rebuild / Remove分類。
- テスト方法。
- iOS / Safe Area上の注意点。
- シナリオデータ形式。
- Save Snapshot設計。
- 小規模で現実的な依存ライブラリ。
- 開発工程の順序。
- 代替案とトレードオフ。

ただし、提案と決定を混同しない。

---

## 18. 判断に迷ったとき

優先順位は次の通り。

1. ユーザーの最新の明示指示に従う。
2. 明示的な変更指示がない限り、`MASTER.md` に書かれた作品の核を守る。
3. Vertical Sliceの完成を優先する。
4. ユーザーの既存コードとデータを壊さない。
5. 小さく、理解しやすく、検証可能に進める。
6. 未決定事項は未決定のまま扱う。

ユーザーの指示によって `MASTER.md` の既存方針を変更する場合は、変更内容と理由が分かるように文書も更新する。

判断不能な場合は、無理に仕様を固定せず、選択肢・影響・推奨案を提示する。
